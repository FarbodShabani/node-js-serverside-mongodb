<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <ul style="display: flex; flex-direction: column; align-items: center;">
                <% if(cartProducts != undefined) { %> 
                <% if(cartProducts?.length > 0) { %> 
                    <% cartProducts.forEach((cartProduct) => { %> 
                        <li style="display: flex; flex-direction: row; justify-content: space-between; align-items: center; width: 30%;">
                          <p>
                              <%= cartProduct.title %> (<%= cartProduct.quantity %> ) 
                          </p>
                          <form action="/cart-delete-item" method="post">
                              <input type="hidden" name="productId" value="<%= cartProduct._id %>">
                              <button type="submit" class="btn">
                                  delete
                              </button>
                          </form>
                        </li>
                        <% })  %> 
                        <hr width="100%"/>
                        <form action="/orders" method="POST">
                            <button type="submit" class="btn">
                                Order Now!!!
                            </button>
                        </form>
                    <% } else {  %> 
                        <h1> THERE IS NO ITEM IN THE CART !!!</h1>
                        <% } %> 
                        <% } %> 
                    </ul>
        </main>
        <%- include('../includes/end.ejs') %>